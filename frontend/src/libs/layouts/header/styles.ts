import styled from 'styled-components'

export const HeaderContainer = styled.header`
  width: 100%;
  padding: 1rem 2rem;
  box-shadow: 0 .2rem .4rem var(--color-grey);
`

export const Title = styled.h1`
  font-size: 2.2rem;
  color: var(--color-blue);
`
